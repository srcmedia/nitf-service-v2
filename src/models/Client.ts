import { LoggerFactory } from "../util/LoggerFactory";

/**
 * Client class, Should be a relatively simple POTO class with accessors for every field
 */
export class Client {
    private LOGGER = LoggerFactory.create('Client');

    constructor() {}
}